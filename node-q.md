# 🚀 Node.js Practice Questions

## 🟢 Level 1: Basics (Warm-up)

1. What is Node.js? How is it different from browser JavaScript?

2. What is the event loop in Node.js?

3. Explain the difference between:
   - synchronous vs asynchronous code

4. What is `require()`? How does module system work in Node.js?

5. Write a simple Node.js script to:
   - print "Hello World"
   - read a file and display its content

---

## 🟡 Level 2: Core Modules

6. Write a program using `fs` module to:
   - create a file
   - write data into it
   - read the file
   - delete the file

7. Build a simple CLI tool:
   - Input: `node app.js add 5 3`
   - Output: `8`

8. Create a server using `http` module that:
   - runs on port 3000
   - returns "Server Running"

9. Modify the server:
   - `/` → Home page
   - `/about` → About page
   - `/contact` → Contact page

---

## 🟠 Level 3: Express.js Basics

10. Install Express and create a basic server.

11. Create routes:
   - GET `/`
   - GET `/users`
   - POST `/users`

12. Create an API:
   - Store users in an array
   - Add user (POST)
   - Get all users (GET)

13. What is middleware? Create:
   - a logger middleware (log every request)

---

## 🔴 Level 4: Real-world Backend Practice

14. Build a simple TODO API:
   - Add task
   - Get all tasks
   - Delete task

15. Add validation:
   - Task should not be empty

16. Use `nodemon` for auto-restart.

17. Handle errors properly:
   - return proper status codes (200, 400, 500)

---

## 🔵 Level 5: Async & Advanced

18. Convert callback code to:
   - Promises
   - async/await

19. Explain:
   - `process.nextTick()`
   - `setImmediate()`

20. What happens internally when you hit an API in Node.js?

---

## ⚫ Level 6: Interview + Thinking Questions

21. Why is Node.js single-threaded but still handles multiple requests?

22. What are advantages of Node.js?

23. When should you NOT use Node.js?

24. What is the difference between:
   - `require` vs `import`

25. What is blocking vs non-blocking code?

---

## 🧠 Bonus Challenge (IMPORTANT)

### Build this mini project:

👉 "User API"

Features:
- Create user
- Get all users
- Get user by ID
- Delete user

Constraints:
- Use Express
- No database (use array)
- Add validation
- Proper error handling

---

## 🎯 How to Use This

- Try solving each question yourself
- If stuck → ask for explanation
- After solving → optimize code

---

## ⚡ If You Want Next Level

After this, ask me:
👉 "Give me Node.js project questions for interview level"

I’ll give you **company-level backend challenges**